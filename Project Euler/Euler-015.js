
const grid = [
    [1, 2],
    [3, 4]
]
console.log(grid[0][1]);

const findRoute = function(grid, size)
{
    let count = 0;
    let i = 0, j = 0;
    let itr = size;
    if(j == size)
    {
        count++;
        for(i = 0, j = 0; itr >= 0; itr--)
        {
            
        }
    }
    (i == size) ? j++ : i++;
    
}